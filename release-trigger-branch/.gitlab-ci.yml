image: $DOCKER_IMAGE

before_script:
  - source ./release.sh
  - initCI

stages:
  - release

tag-release:
  stage: release
  tags:
    - docker
  script:
    # source maven-auto-releaser script
    # - source ./release.sh
    # call tagRelease from maven-auto-releaser script
    - tagRelease
  only:
    - $RELEASE_TRIGGER_BRANCH

release:
  stage: release
  tags:
    - docker
  script:
    # source maven-auto-releaser script
    # - source ./release.sh
    # call prepareRelease from maven-auto-releaser script
    - prepareRelease
    # call executeRelease from maven-auto-releaser script
    - executeRelease
  only:
    - tags
